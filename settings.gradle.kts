rootProject.name = "CMPImagePickNCrop"
include(":composeApp")
include(":cmpimagepickncrop")

pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}
